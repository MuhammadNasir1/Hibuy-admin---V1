<div class="w-full">
    <label for="{{ $id }}" class="block mb-1 text-sm font-medium text-customBlack ">{{ $label }}</label>
    <input value="{{$value}}" type="{{ $type }}" name="{{ $name }}" id="{{ $id }}" placeholder="{{ $placeholder }}"
        class=" border border-gray-300 text-gray-900  text-sm rounded-md focus:ring-primary focus:border-primary block w-full p-2.5"
        required />
</div>
